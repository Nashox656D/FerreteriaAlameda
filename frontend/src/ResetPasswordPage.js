// ResetPasswordPage removed: automatic email/password reset flow was reverted.
// The application now uses the admin-driven `Recuperacion` flow (create a request
// under `api/recuperacion/`) which administrators process from the UI.

export default function ResetPasswordPage() { return null; }
