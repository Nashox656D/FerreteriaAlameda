# Example .env for backend (copy to .env and update values)
# SECURITY
SECRET_KEY=replace-me-with-a-secure-random-string
DEBUG=True

# Database (Postgres example)
# DATABASE_URL=postgres://user:password@host:port/dbname
PGDATABASE=your_db_name
PGUSER=your_db_user
PGPASSWORD=your_db_password
PGHOST=localhost
PGPORT=5432

# Company / project specific
DEFAULT_COMPANY_NAME=Ferreteria Alameda
