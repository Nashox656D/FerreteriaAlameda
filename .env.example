# This example ENV was used for the removed email-based password reset flow.
# The automatic email/password-reset feature was reverted; the project now uses
# the admin-driven `Recuperacion` flow. Keep or remove these vars as you wish.

# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=tu.cuenta@gmail.com
# EMAIL_HOST_PASSWORD=tu_app_password_de_16_caracteres
# DEFAULT_FROM_EMAIL=no-reply@tudominio.com

# PASSWORD_RESET_TIMEOUT=3600
# FRONTEND_URL=http://localhost:3000